<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.new9.shitlecture.Activity.LoginActivity">

    <EditText
        android:id="@+id/idEditText"
        android:layout_width="280dp"
        android:layout_height="50dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:hint="ID"
        android:text="2014104128"
        android:textColor="#000000"
        android:textColorHint="@color/dim_foreground_disabled_material_light" />

    <EditText
        android:id="@+id/pwEditText"
        android:layout_width="280dp"
        android:layout_height="50dp"
        android:layout_alignLeft="@id/idEditText"
        android:layout_below="@id/idEditText"
        android:hint="PASSWORD"
        android:text="139dkssud!"
        android:textColor="#000000"
        android:textColorHint="@color/dim_foreground_disabled_material_light" />

    <Button
        android:id="@+id/loginButton"
        style="?android:attr/borderlessButtonStyle"
        android:layout_width="130dp"
        android:layout_height="55dp"
        android:layout_alignLeft="@id/idEditText"
        android:layout_below="@id/pwEditText"
        android:background="#00ff0000"
        android:onClick="loginClicked"
        android:text="Log in"
        android:textAllCaps="true"
        android:textColor="#000000" />

    <Button
        android:id="@+id/signupButton"
        style="?android:attr/borderlessButtonStyle"
        android:layout_width="130dp"
        android:layout_height="55dp"
        android:layout_alignEnd="@+id/pwEditText"
        android:layout_alignRight="@+id/pwEditText"
        android:layout_below="@+id/pwEditText"
        android:background="#00ff0000"
        android:onClick="signUpClicked"
        android:text="Sign up"
        android:textAllCaps="true"
        android:textColor="#000000" />

</RelativeLayout>
